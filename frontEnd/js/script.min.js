/*! frontend 2021-09-14 */

console.log("js is all linked up yay!");let url;$.ajax({url:"js/config.json",type:"GET",dataType:"json",success:function(a){url=`${a.SERVER_URL}:${a.SERVER_PORT}`},error:function(a){alert("Error:",a)}}),"student-profiles-page"===$("body").data("title")&&window.addEventListener("load",()=>{$.ajax({url:`${url}/allStudents`,type:"GET",dataType:"json",success:function(a){console.log("getting all students... :)");let s=3,d=0;for(var e=0;e<a.length;e++)s%3==0&&(console.log(`cardcount is at ${s}`),document.querySelector(".carousel-inner").innerHTML+=`
              <div class="carousel-item">
                <div class="card-group mx-auto student-cards">
                  <div class="card">
                    <div class="card-body">
                      <h5 class="card-title">${a[d].name}</h5>
                      <h6>${a[d].username} • ${a[d].educator}</h6>
                      <img src="${a[d].pfpUrl}" alt="">
                      <h5>${a[d].studyField}</h5>
                      <p class="card-text">${a[d].extra}</p>
                    </div>
                  </div>
                  <div class="card">
                    <div class="card-body">
                    <h5 class="card-title">${a[d+1].name}</h5>
                    <h6>${a[d+1].username} • ${a[d+1].educator}</h6>
                    <img src="${a[d+1].pfpUrl}" alt="">
                    <h5>${a[d+1].studyField}</h5>
                    <p class="card-text">${a[d+1].extra}</p>
                  </div>
                    </div>
                  </div>
                  <div class="card">
                    <div class="card-body">
                    <div class="card-body">
                    <h5 class="card-title">${a[d+2].name}</h5>
                    <h6>${a[d+2].username} • ${a[d+2].educator}</h6>
                    <img src="${a[d+2].pfpUrl}" alt="">
                    <h5>${a[d+2].studyField}</h5>
                    <p class="card-text">${a[d+2].extra}</p>
                    </div>
                  </div>
                </div>
              </div>
              `),s+=1,d+=3},error:function(){alert("Error: Cannot GET")}})});